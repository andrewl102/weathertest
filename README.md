weathertest
===========

To build the application, use mvn clean package.
This builds a war that can be tested with Tomcat or Jetty.

For a easier way of deploying the application, run mvn:jetty-run and then access http://localhost:8080/test/form .
